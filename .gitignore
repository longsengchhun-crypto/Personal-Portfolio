__pycache__/
*.py[cod]
db.sqlite3
.env
.env.local
.next/
node_modules/
*.tsbuildinfo
media/
staticfiles/
.venv/
venv/
runserver*.log
next-dev-*.log

# Legacy Django static output — superseded by public/static/ in the Next.js app.
# Anchored to the repo root so this never matches public/static/.
/static/

# (The showreel mp4 in public/ is now a compressed ~7MB copy and is tracked so Git deploys serve it.)

.vercel
.tmp-vercel*
supabase/.temp/
